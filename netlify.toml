[build]
  command = "npm install --legacy-peer-deps; npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20.5.0"
  CI = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
